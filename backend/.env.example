MONGO_URI=mongodb+srv://user:password@cluster.mongodb.net/superbeach?retryWrites=true&w=majority
JWT_SECRET=your-jwt-secret
PORT=8000

# Keep-alive (Render free tier) — ativo automaticamente em NODE_ENV=production
KEEP_ALIVE_URL=https://superbeach.onrender.com/
KEEP_ALIVE_INTERVAL_MS=30000
# KEEP_ALIVE_ENABLED=true
